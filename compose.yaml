# docker-compose.yaml
services:
  llm_server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_server
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./.env:/app/.env

    env_file:
      - .env
